┌─────────────────────────────────────────────────────────┐
│                  用户请求                                 │
│ "把负荷水平调高2倍，潮流计算是否出现线路过载？"            │
│ "再调高4倍呢？"                                         │
└──────────────┬──────────────────────────────────────────┘
               │
   ┌───────────▼────────────────────────────────────┐
   │            意图识别层                            │
   │  _is_load_sweep_skill() 关键词匹配               │
   │  触发词：负荷倍率/负荷水平/调高/几倍/倍率 ...      │
   └───────────┬────────────────────────────────────┘
               │
      ┌────────▼────────┐
      │   两种执行模式    │
      └────────┬────────┘
               │
    ┌──────────┴──────────┐
    ▼                     ▼
┌─────────────┐    ┌──────────────────┐
│ 模式A：批量  │    │ 模式B：问答式      │
│ Skill模式   │    │ 交互模式          │
└──────┬──────┘    └────────┬──────────┘
       │                    │
       │                    │
  ┌────▼────────────────────▼────────────────────────────┐
  │                 模式A：批量Skill执行                   │
  │         _run_skill_load_sweep()                      │
  │                                                      │
  │  LOAD_SWEEP_SKILL 预定义步骤:                          │
  │                                                      │
  │  Step 1: set_load_scale(factor=2.0)  ─→  负荷放大2倍   │
  │  Step 2: run_ac_power_flow()         ─→  执行潮流计算   │
  │  Step 3: get_line_overload_summary   ─→  检测2倍过载   │
  │          (skip_runpp=True)                            │
  │  Step 4: set_load_scale(factor=4.0)  ─→  负荷放大4倍   │
  │  Step 5: run_ac_power_flow()         ─→  执行潮流计算   │
  │  Step 6: get_line_overload_summary   ─→  检测4倍过载   │
  │          (skip_runpp=True)                            │
  │                                                      │
  │  → 统一组装结果：场景日志 + 各倍率过载对比              │
  └──────────────────────────────────────────────────────┘
       │                    │
       │                    │
  ┌────▼────────────────────────────────────────────────┐
  │                 模式B：问答式交互执行                    │
  │         _run_skill_load_sweep_step()                 │
  │                                                      │
  │  用户可以分步追问:                                     │
  │                                                      │
  │  第1轮："负荷调高2倍，潮流计算是否出现线路过载？"        │
  │    ├─ extract_load_factor() → factor=2.0              │
  │    ├─ set_load_scale(2.0)     负荷放大2倍             │
  │    ├─ run_ac_power_flow()     潮流计算                │
  │    └─ get_line_overload_summary(skip_runpp=True)      │
  │                                 检测过载              │
  │                                                      │
  │  第2轮："那4倍呢？"                                    │
  │    ├─ extract_load_factor() → factor=4.0              │
  │    ├─ set_load_scale(4.0)     负荷放大4倍             │
  │    ├─ run_ac_power_flow()     潮流计算                │
  │    └─ get_line_overload_summary(skip_runpp=True)      │
  │                                 检测过载              │
  │                                                      │
  │  特性：                                                │
  │  - 已完成场景自动缓存，不重复计算                        │
  │  - 支持多倍率对比查看                                    │
  │  - 支持混合查询（潮流+过载同时展示）                     │
  │  - 结束后自动恢复负荷至1.0（finally块）                   │
  └──────────────────────────────────────────────────────┘