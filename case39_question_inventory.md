# 面向 pandapower case39（IEEE 39 节点）模型 · 可问问题清单

> 用途：为《城市电网运行态势感知与协同调度决策》测评做准备，枚举一个 case39 智能体可被问到的问题，并对照现有 7 类静态安全分析工具的能力缺口。
> 依据：附件"三、测试任务示例" + pandapower case39 模型本身可计算的量。

---

## 一、测评文档"测试任务示例"原始 9 条

1. IEEE-39 节点系统中**线路 11**连接哪两个母线？
2. 母线电压正常运行范围是多少？
3. N-1 静态安全校核需要检查哪些越限类型？
4. 某个潮流计算工具需要输入哪些参数？
5. 对 IEEE-39 节点系统**运行交流潮流，并输出有功网损**；
6. **筛选负载率最高的 5 条线路**；
7. 对**线路 171**开展 N-1 校核；
8. 对**关键线路逐一进行故障分析并排序**；
9. 根据仿真结果输出**母线低电压、线路过载等风险及其证据**。

> 测评考察维度（原文）：识别分析对象、运行方式、计算方法、筛选范围、输出要求。

---

## 二、可问问题枚举（按能力维度分类）

### A. 识别分析对象 —— 拓扑 / 结构 / 元件查询
> 当前 7 类工具**不支持**，需新增"电网拓扑查询"类工具。

- IEEE-39 系统中**线路 N（如 11、171）连接哪两个母线**？
- 系统共有多少个**母线 / 线路 / 变压器 / 发电机 / 负荷**？
- 列出所有**母线名称及编号**。
- 哪条母线是**平衡节点（slack/ext_grid）**？其余是什么类型？
- 列出**所有发电机**及其所接母线、额定容量。
- 列出**所有变压器**及其高/低压侧母线、额定容量。
- 系统的**总负荷**（有功/无功）是多少？最大负荷在哪个母线？
- 某条线路（如 171）的**电气参数**：长度、型号、电阻 R、电抗 X、容量上限？
- 某台变压器/发电机的**额定参数**与当前设定值？
- 母线 X 与母线 Y 之间**是否连通**？有几条电气路径？
- 若断开某条线路，**网络是否解列**（某母线是否会失电）？

### B. 运行方式查询 —— 元件参数 / 当前工况
> 部分可借现有工具结果呈现，但"指定元件参数"查询当前缺失。

- 各母线的**基准电压等级**是多少（如 230 kV）？
- 发电机 G 的当前**有功/无功出力设定**与电压设定？
- 负荷 L 的有功/无功**当前值**？
- 当前运行方式下，系统**总发电功率、总负荷、总损耗**各是多少？

### C. 计算方法 / 知识查询 —— 元信息与规则
> 当前 7 类工具**不支持**，需新增"知识/说明"类能力（可用 LLM 内置知识或配置回答）。

- 母线电压**正常运行范围**是多少（如 0.95~1.05 pu）？
- N-1 静态安全校核需要检查哪些**越限类型**（电压越限、线路过载）？
- 交流潮流计算需要**输入哪些参数**（算法、迭代次数、收敛精度）？
- 电压稳定裕度**是什么含义**？如何计算？
- 网损率**如何计算**（损耗/总供电）？
- 短路计算有哪几种**故障类型**（三相/两相/单相）？
- 什么是 N-1 准则？为什么做静态安全校核？

### D. 仿真计算执行 —— 潮流 / N-1 / 短路 / 稳定 / 网损
> 现有 7 类工具**主体已覆盖**，但需支持"指定指标输出"与"指定元件对象"。

- 对 IEEE-39 运行**交流潮流**，并输出**有功网损**（示例 5）。
- 输出各**母线电压**及是否越限。
- 输出各**发电机出力**与各**线路潮流**。
- 系统**平均/最低/最高电压**是多少？
- 线路/变压器**最大负载率**是多少？
- 对**线路 171**（指定元件）开展 N-1 校核（示例 7）。
- 对所有**线路 / 变压器 / 母线**逐一做 N-1 校核并统计不安全项。
- 对**母线 X**做三相/两相/单相**短路计算**，短路电流多大？
- 比较各母线**短路电流水平**，哪几个母线最危险？
- 计算 IEEE-39 的**电压稳定裕度**与 P-V 曲线。
- 计算系统**网损**（线路+变压器损耗、网损率）。

### E. 筛选与排序 —— 范围筛选 / Top-N
> 现有 `get_line_overload_summary` 等只做阈值统计，**缺少"Top-N 排序"能力**。

- **筛选负载率最高的 5 条线路**（示例 6）。
- 列出**所有过载线路**（负载率 > 阈值）。
- 找出**电压最低的 3 个母线** / 越限母线。
- 按**有功损耗**对线路排序，损耗最大的几条是哪些？
- 按**有功出力**对发电机排序。
- 找出**最接近过载**的 N 条线路（裕度最小）。

### F. 风险识别与证据输出
> 当前只返回数据，**缺少"风险条目 + 证据"的结构化生成**。

- 根据仿真结果输出**母线低电压、线路过载等风险及其证据**（示例 9）。
- 输出 N-1 校核中**所有不收敛/越限的具体元件及数值证据**。
- 给出本次分析的**安全性结论**（安全/基本安全/有风险/不安全）与依据。

### G. 多步 / 复合任务
> 当前单轮单工具，**不支持多步编排**，需新增"任务规划 / 多步执行"能力。

- 对**关键线路逐一进行故障分析并排序**（示例 8）。
- 先跑**潮流** → 再**筛选过载线路** → 最后**输出风险报告**。
- 对比 **N-1 前后**系统指标（电压、负载率）变化。
- 先识别**负载率最高的 5 条线路**，再对这 5 条**逐条做 N-1 校核**并汇总。

---

## 三、能力缺口对照（现有 7 类工具 vs 测评需求）

| 测评需要的能力 | 示例问题 | 当前工具是否覆盖 | 需新增/扩展 |
|---|---|---|---|
| 拓扑/结构查询 | 线路11连哪两母线、列母线 | ❌ 无 | 电网拓扑查询工具 |
| 元件参数查询 | 线路171参数、发电机设定 | ❌ 弱 | 元件参数读取 |
| 知识/元信息 | 电压范围、工具参数 | ❌ 无 | 知识问答（LLM/配置） |
| 指定指标输出 | 输出有功网损 | ⚠️ 部分 | 工具结果按指标提取 |
| 指定元件对象 | 对线路171做N-1 | ⚠️ 现按整网 | 支持 element_id 定向 |
| Top-N 排序/筛选 | 负载率最高5条 | ⚠️ 仅阈值 | 排序/Top-N 能力 |
| 风险+证据 | 输出风险及证据 | ❌ 无 | 风险结构化生成 |
| 多步复合 | 逐线故障并排序 | ❌ 无 | 任务规划/多步执行 |
| 潮流/N-1/短路/稳定/网损 | 整网分析 | ✅ 已有 | 复用+细化 |

**结论**：现有 7 类工具覆盖了"整网仿真计算"内核，但测评真正拉开差距的是——
1. **拓扑/元件查询类**（让 agent 能"识别分析对象"）；
2. **指定元件 + Top-N 排序/筛选类**（"筛选范围"）；
3. **知识/元信息类**（"计算方法"）；
4. **风险证据 + 多步复合类**（"输出要求"与任务编排）。
这四类应是工具扩充的重点方向。
