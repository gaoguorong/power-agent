# ⚡ 电网分析智能体 - 启动指南（Windows）

## 📋 改造完成内容

- ✅ **后端**：http.server → **FastAPI**（含自动文档/SSE流式接口）
- ✅ **会话**：单会话内存 → **MySQL 业务元数据 + SQLite LangGraph Checkpoint** 双层持久化
- ✅ **状态隔离**：每个会话独立电网对象 + 懒恢复（重启服务自动按元信息恢复）
- ✅ **安全**：API Key / DB 密码 → **.env 环境变量**，无硬编码
- ✅ **前端**：单文件 HTML → **Vue 3 + Element Plus + Pinia**（ChatGPT 风格三栏布局）
  - 左侧：会话列表 + 搜索 + 新建/重命名/删除
  - 中间：欢迎页/消息流/打字机效果/工具调用链详情
  - 右侧：（无需）输入区含"电网选择下拉 + 快捷问题芯片 + Enter发送"
- ✅ **流式聊天**：SSE 推送 `welcome / tool_start / tool_end / token / done / error` 6种事件

---

## 🚀 启动步骤（按顺序来）

### Step 0. 安装依赖（只需一次）

#### 后端 Python 依赖
```powershell
cd 项目根目录
pip install -r requirements.txt
```

#### 前端 Node 依赖（只需第一次）
- 安装 Node 18+（没装去 https://nodejs.org/ 下 LTS 版）
- 双击 `install_frontend.bat` 或手动：
```powershell
cd frontend
npm config set registry https://registry.npmmirror.com   # 国内建议切淘宝镜像，加速
npm install
```

---

### Step 1. 配置环境变量 `.env`

把根目录下的 **`.env.example` 复制一份，改名为 `.env`**，然后填写：

```env
# 1) MySQL：必须先在MySQL里执行  CREATE DATABASE power_agent DEFAULT CHARSET utf8mb4;
MYSQL_URL=mysql+aiomysql://root:你的密码@127.0.0.1:3306/power_agent?charset=utf8mb4

# 2) LLM：替换为实际 Key/地址（阿里百炼/DeepSeek/任意兼容OpenAI格式均可）
LLM_API_KEY=sk-xxxxxxxxxxxxxxxxxx
LLM_API_URL=https://xxx.cn-beijing.maas.aliyuncs.com/compatible-mode/v1/chat/completions
LLM_API_URL_LANGCHAIN=https://xxx.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
LLM_MODEL=deepseek-v4-pro-0813
```

---

### Step 2. 启动后端

双击 `run_server.bat`，或命令行：
```powershell
python main.py
```

打开浏览器访问 Swagger 验证后端是否正常：  
👉 **http://localhost:8000/docs**

- 先点 `GET /api/health`，看到 `mysql_available=true, llm_available=true` 就搞定

---

### Step 3. 启动前端

双击 `run_frontend.bat`，或命令行：
```powershell
cd frontend
npm run dev
```

浏览器会自动打开前端地址：  
👉 **http://localhost:5173**

> 注：Vite 已将 `/api` 代理到 `http://127.0.0.1:8000`，前端代码无需再写后端地址。

---

## 🗂 项目结构

```
power-agent/
├── main.py                     # 服务入口（组合根）：只装配应用，不含具体接口
├── controllers/                # C 层：HTTP 路由（只有 get/post 等接口编排）
│   ├── config_controller.py    # 前端初始化配置项（/api/config/*）
│   ├── session_controller.py   # 会话 CRUD + 电网重置（/api/sessions/*）
│   ├── chat_controller.py      # SSE 流式聊天接口
│   └── frontend_controller.py  # 前端 SPA 静态文件托管
├── schemas/                    # DTO 层：Pydantic 请求/响应模型（原 schemas.py）
│   ├── common.py               # 统一响应包装 ok/fail/ApiResponse
│   ├── session.py              # 会话相关 DTO
│   ├── chat.py                 # 聊天相关 DTO
│   └── options.py              # 前端选项 DTO
├── services/                   # 业务层
│   ├── graph_service.py        # GraphAgent 单例：手动驱动状态机 + SSE事件产出
│   ├── session_service.py      # 会话业务（MySQL+GraphAgent串起来+懒恢复）
│   └── memory_store.py         # MySQL 不可用时的内存降级存储
├── models/                     # M 层：数据持久化（原 db/）
│   ├── database.py             # SQLAlchemy 异步连接、建表
│   ├── session.py              # PowerSession ORM 模型
│   └── session_repository.py   # 会话 CRUD 封装
├── agents/                     # Agent 层
│   ├── graph_agent.py          # 核心智能体：LangGraph 状态机（原 GraphAgent.py）
│   └── llm_client.py           # LLM 实例工厂（原 llm_client.py）
├── tools/                      # 独立工具函数（LLM 可调用的 @tool）
│   ├── grid_tools.py           # 电网计算（pandapower 封装）
│   └── langchain_tool.py       # LangChain @tool 薄壳
├── config/
│   ├── model_config.py         # 后端配置（环境变量/阈值/知识库/提示词）
│   └── app_options.py          # 前端选项（电网下拉框/快捷问题）
├── skills/
│   └── skill_def.py            # 复合技能定义（电压修正/负荷扫描）
├── mcp/
│   └── mcp_server.py           # MCP 协议服务（独立进程）
├── scripts/
│   └── langchain_v2_demo.py    # 独立演示脚本（原 langchain_v2.py）
├── frontend/                   # Vue 3 前端
│   ├── src/
│   │   ├── App.vue             # 三栏布局（顶栏+侧边栏+聊天区）
│   │   ├── main.ts
│   │   ├── api/index.ts        # axios 接口封装
│   │   ├── stores/session.ts   # Pinia 状态（会话/消息/SSE 处理）
│   │   └── components/
│   │       ├── Sidebar.vue           # 左侧会话列表
│   │       ├── ChatWindow.vue        # 中间聊天窗口
│   │       ├── ChatMessageItem.vue   # 单条消息气泡（含工具链）
│   │       ├── ToolOutputView.vue    # 工具输出（表格/JSON/文本）
│   │       └── ConfigPanel.vue       # 会话级配置弹窗
│   └── package.json
├── .env.example              # 环境变量模板（复制为.env填写）
├── install_frontend.bat      # 一键装前端依赖（Windows）
├── run_server.bat            # 一键启动后端（Windows）
├── run_frontend.bat          # 一键启动前端（Windows）
└── requirements.txt
```

---

## 🧪 快速冒烟测试流程

1. 启动后端 `python main.py`，确保 health 接口都返回 true
2. 启动前端 `npm run dev`，打开 5173
3. 左侧会自动新建一个会话 → 顶部显示"当前会话：新会话"
4. 中间欢迎页点一个快捷问题（比如"潮流计算"）
5. 点发送 → 能看到
   - 左侧出现"🔄 正在执行：create_test_grid" → ✅ 结束
   - 再出现工具"run_ac_power_flow"
   - AI 文字以打字机方式输出
   - 回答里工具调用链是一个可折叠卡片，展开能看到表格结果

---

## 🔌 接口清单（FastAPI 路由）

| Method | Path | 作用 |
|---|---|---|
| GET | `/api/health` | 健康检查（含MySQL、LLM状态） |
| GET | `/api/config/options` | 电网下拉/快捷问题/默认配置 |
| POST | `/api/sessions` | 新建会话 |
| GET | `/api/sessions` | 会话列表（侧边栏） |
| GET | `/api/sessions/{id}` | 单会话详情 + 历史消息 |
| PATCH | `/api/sessions/{id}` | 修改会话名 / 配置 |
| DELETE | `/api/sessions/{id}` | 删除会话（软删） |
| POST | `/api/sessions/{id}/reset` | 重置电网 |
| POST | `/api/sessions/{id}/chat/stream` | **核心**：SSE 流式聊天 |

详细请求/响应示例：打开 `http://localhost:8000/docs` Swagger 直接在线调用。